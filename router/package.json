{
  "name": "nightbear-router",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "engines": {
    "npm": "~3.10",
    "node": "~6.10"
  },
  "scripts": {
    "lint": "check-node-version --package && tslint --project . --type-check && echo lint OK",
    "lint-watch": "chokidar '**/*.ts' --ignore node_modules --debounce 100 --initial --command 'npm run --silent lint'",
    "test": "check-node-version --package && mocha --require ts-node/register $(npm run --silent test-list)",
    "test-watch": "mocha --require ts-node/register --watch --watch-extensions ts $(npm run --silent test-list)",
    "test-list": "find . -name '*.spec.ts' -not -path './node_modules/*'",
    "precommit": "npm run lint && npm test"
  },
  "author": "Marja Käpyaho <marja.kapyaho@iki.fi>",
  "contributors": [
    "Jarno Rantanen <jarno@jrw.fi>"
  ],
  "license": "AGPL",
  "dependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.43",
    "@types/node": "^8.0.34",
    "axios": "^0.17.1",
    "chai": "^4.1.2",
    "check-node-version": "^3.1.1",
    "chokidar-cli": "^1.2.0",
    "husky": "^0.14.3",
    "mocha": "^4.0.1",
    "ts-node": "^3.3.0",
    "tslint": "^5.7.0",
    "typescript": "^2.6.2"
  }
}
