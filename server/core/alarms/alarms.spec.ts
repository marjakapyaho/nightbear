import 'mocha';
import { assert } from 'chai';

describe('utils/analyser-utils', () => {

  // Mock objects

  // Assertations
  it('does not create any alarms', () => {
    assert.deepEqual(
      '',
      '',
    );
  });

});
