{
  "name": "nightbear-server",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "engines": {
    "npm": ">=5.4.2 <6",
    "node": ">=8.7.0 <9"
  },
  "scripts": {
    "start": "ts-node index.ts",
    "lint": "tslint --project . --type-check && echo lint OK",
    "lint-watch": "chokidar '**/*.ts' --ignore node_modules --debounce 100 --initial --command 'npm run --silent lint'",
    "test": "mocha --require ts-node/register $(npm run --silent test-list)",
    "test-watch": "mocha --require ts-node/register --watch --watch-extensions ts $(npm run --silent test-list)",
    "test-list": "find . -name '*.spec.ts' -not -path './node_modules/*'"
  },
  "author": "Marja Käpyaho <marja.kapyaho@iki.fi>",
  "contributors": [
    "Jarno Rantanen <jarno@jrw.fi>"
  ],
  "license": "AGPL",
  "dependencies": {
    "@types/body-parser": "^1.16.5",
    "@types/chai": "^4.0.4",
    "@types/cors": "^2.8.1",
    "@types/express": "^4.0.37",
    "@types/mocha": "^2.2.43",
    "@types/node": "^8.0.34",
    "body-parser": "^1.18.2",
    "chai": "^4.1.2",
    "chokidar-cli": "^1.2.0",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "mocha": "^4.0.1",
    "ts-node": "^3.3.0",
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  }
}
